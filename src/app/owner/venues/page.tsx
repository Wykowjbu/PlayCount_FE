'use client';
import { useState } from 'react';
import { Button } from '@/components/playcourt/button';
import { Input } from '@/components/playcourt/input';
export default function OwnerVenuesPage() { const [courts, setCourts] = useState([{ name: 'Court A', active: true }, { name: 'Court B', active: true }]); const [newCourt, setNewCourt] = useState(''); const [open, setOpen] = useState(false); return <section className="mx-auto max-w-5xl px-6 py-10"><p className="font-mono text-xs uppercase tracking-[.16em] text-[var(--pc-mute)]">Venue settings</p><h1 className="mt-2 text-3xl font-semibold text-[var(--pc-ink)]">Quản lý địa điểm & sân</h1><div className="mt-7 grid gap-6 md:grid-cols-2"><form className="space-y-4 rounded-xl border border-[var(--pc-hairline)] bg-white p-6"><Input label="Tên địa điểm" defaultValue="PlayCourt Phú Nhuận" /><label className="block text-sm font-medium">Mô tả<textarea className="mt-2 w-full rounded-md border border-[var(--pc-hairline)] p-2" defaultValue="Sân thể thao trong nhà." /></label><p className="text-sm font-medium">Môn thể thao</p><div className="flex gap-2"><Button variant="Secondary" type="button">Cầu lông</Button><Button variant="Secondary" type="button">Pickleball</Button></div><Button type="button">Lưu thay đổi</Button></form><div className="rounded-xl border border-[var(--pc-hairline)] bg-white p-6"><div className="flex items-center justify-between"><h2 className="font-semibold text-[var(--pc-ink)]">Danh sách sân</h2><Button type="button" onClick={() => setOpen(true)}>Thêm sân</Button></div><ul className="mt-4 divide-y divide-[var(--pc-hairline)]">{courts.map((court, index) => <li key={court.name} className="flex items-center justify-between py-3 text-sm"><span>{court.name}</span><label className="flex items-center gap-2 text-[var(--pc-body)]"><input type="checkbox" checked={court.active} onChange={() => setCourts((items) => items.map((item, itemIndex) => itemIndex === index ? { ...item, active: !item.active } : item))} /> Hoạt động</label></li>)}</ul></div></div>{open && <div role="dialog" aria-modal="true" className="fixed inset-0 grid place-items-center bg-black/30 p-4"><form className="w-full max-w-sm rounded-xl bg-white p-6" onSubmit={(event) => { event.preventDefault(); if (newCourt.trim()) setCourts((items) => [...items, { name: newCourt, active: true }]); setNewCourt(''); setOpen(false); }}><h2 className="font-semibold">Thêm sân</h2><Input label="Tên sân" value={newCourt} onChange={(event) => setNewCourt(event.target.value)} /><div className="mt-5 flex justify-end gap-2"><Button type="button" variant="Secondary" onClick={() => setOpen(false)}>Hủy</Button><Button type="submit">Tạo sân</Button></div></form></div>}</section>; }
