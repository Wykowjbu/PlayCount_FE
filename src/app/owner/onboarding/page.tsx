'use client';
import { useState } from 'react';
import { Button } from '@/components/playcourt/button';
import { Input } from '@/components/playcourt/input';
import { StatusBadge } from '@/components/playcourt/status-badge';
export default function OwnerOnboardingPage() { const [submitted, setSubmitted] = useState(false); const [fileName, setFileName] = useState(''); return <section className="mx-auto max-w-3xl px-6 py-10"><p className="font-mono text-xs uppercase tracking-[.16em] text-[var(--pc-mute)]">Owner KYC</p><div className="mt-2 flex items-center justify-between"><h1 className="text-3xl font-semibold text-[var(--pc-ink)]">Xác minh chủ sân</h1><StatusBadge status={submitted ? 'pending' : 'draft'} /></div><form className="mt-7 space-y-5 rounded-xl border border-[var(--pc-hairline)] bg-white p-6" onSubmit={(event) => { event.preventDefault(); setSubmitted(true); }}><Input required label="Tên doanh nghiệp / chủ sân" placeholder="PlayCourt Phú Nhuận" /><Input required label="Người đại diện" placeholder="Nguyễn Văn An" /><label className="block text-sm font-medium text-[var(--pc-ink)]">Giấy phép kinh doanh<input className="mt-2 block w-full text-sm" type="file" accept="image/*,.pdf" onChange={(event) => setFileName(event.target.files?.[0]?.name ?? '')} /></label>{fileName && <div className="rounded-md bg-[var(--pc-green-50)] p-3 text-sm text-[var(--pc-green-900)]">Đã tải: {fileName}<div className="mt-2 h-1.5 overflow-hidden rounded bg-[var(--pc-green-100)]"><div className="h-full w-full bg-[var(--pc-green-700)]" /></div></div>}<Button type="submit">Gửi hồ sơ xác minh</Button></form>{submitted && <p className="mt-4 rounded-md bg-[var(--pc-warning-soft)] p-3 text-sm text-[var(--pc-warning-deep)]">Hồ sơ đã gửi. Chúng tôi sẽ phản hồi nếu cần bổ sung thông tin.</p>}</section>; }
